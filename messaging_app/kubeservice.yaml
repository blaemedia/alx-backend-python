apiVersion: v1
kind: Service
metadata:
  name: django-messaging-service
spec:
  type: ClusterIP
  selector:
    app: django-messaging-app
    version: blue   # initially routes to blue
  ports:
    - port: 8000
      targetPort: 8000
